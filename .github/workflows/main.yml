name: ساخت APK فوروارد پیامک

# The APK is built here so the shop owner never has to install Android Studio.
on:
  workflow_dispatch:
  push:
    paths:
      - 'android-sms-forwarder/**'
      - '.github/workflows/android-apk.yml'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'

      - name: Build debug APK
        working-directory: android-sms-forwarder
        run: gradle assembleDebug --no-daemon --stacktrace

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: vpnshop-sms-forwarder
          path: android-sms-forwarder/app/build/outputs/apk/debug/*.apk
          if-no-files-found: error
