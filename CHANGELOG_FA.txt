v0.2.2
- پشتیبانی خودکار از کلیدهای قدیمی OxaPay: اگر API جدید کلید را رد کند، درخواست روی API قدیمی فرستاده می‌شود
- پذیرش فرمت camelCase در webhook درگاه (orderId و trackId) برای API قدیمی
- دستور vpnshop oxapay هر دو API را تست می‌کند و می‌گوید کلید شما از کدام نسل است
- حالت sandbox روی API قدیمی با merchant اختصاصی، بدون درگیر کردن حساب واقعی

v0.2.1
- افزودن سرویس با لینک کانفیگ برای کاربرانی که از جای دیگری خرید کرده‌اند
- مچ کردن کانفیگ با پنل بر اساس UUID (یا password در trojan و ss)
- نمایش حجم مصرفی، حجم باقی‌مانده و زمان باقی‌مانده به صورت زنده از پنل
- رفع کرش ربات وقتی توکن ثبت نشده یا webhook فعال باشد
- نمایش پیام واقعی خطای OxaPay به جای کد HTTP خام

v0.2.0
- نصب یک‌دستوری روی سرور با scripts/install.sh
- سرویس‌های systemd برای پنل و ربات با ری‌استارت خودکار و اجرا بعد از ریبوت
- مسیر پنل مدیریت به یک مسیر مخفی تصادفی منتقل شد (ADMIN_PATH)
- دستور مدیریتی vpnshop برای info/status/restart/logs/update/newpath/passwd
- دستور panelinfo برای پیدا کردن دوباره آدرس پنل
- پنل مدیریت گرافیکی جدید با django-unfold و داشبورد آماری

v0.1.6
- رفع خطای 3x-ui در ساخت کلاینت: tgId اکنون به صورت عددی ارسال می‌شود.
- پاکسازی نوع داده فیلدهای عددی payload قبل از ارسال به 3x-ui.
- اصلاح xui_probe برای ارسال tgId=0 به‌جای رشته خالی.
